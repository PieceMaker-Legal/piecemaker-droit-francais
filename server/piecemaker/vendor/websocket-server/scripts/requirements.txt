# Core dependencies (required for pattern-based recognition)
regex>=2026.1.15
tldextract>=5.3.1
pydantic>=2.12.5
PyYAML>=6.0.3

# Document conversion dependencies
markitdown[all]>=0.1.4  # Markitdown with all optional dependencies
pypdf>=6.6.2  # PDF text layer inspection (actively maintained fork of PyPDF2)

# GLiNER2/GLiNER2.5 entity extraction. Since 2.0, local inference dependencies
# (torch, transformers...) live behind the `local` extra.
gliner2[local]>=2.0.0
huggingface-hub>=0.20.0  # Required for model downloading with progress

# Presidio + GLiNER integration
presidio-analyzer>=2.2.0
spacy>=3.7.0

# Accélération GPU de l'encodeur (macOS) — optionnelle : le runtime retombe sur
# torch si coremltools ou le .mlmodelc manquent. Sert à build_coreml.py et à
# presidio-gliner/coreml_runtime.py.
coremltools>=8.0 ; sys_platform == "darwin"
#
# After installing spacy, download language models:
#   python -m spacy download fr_core_news_sm  # French (15MB, 88% NER accuracy)
#   python -m spacy download en_core_web_sm   # English (13MB, 85% NER accuracy)
#
# Or use: python warmup.py  # Downloads all models automatically
